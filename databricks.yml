# This is a Databricks asset bundle definition for demo_repo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: demo_repo
  uuid: 1d50a3f2-f3a8-4755-bb69-ea0cc404c734

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3436593258826689.9.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3436593258826689.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/vijay.yarlagadda@lockton.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vijay.yarlagadda@lockton.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: vijay.yarlagadda@lockton.com
        level: CAN_MANAGE
